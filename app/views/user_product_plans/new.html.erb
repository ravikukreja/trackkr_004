<%= render 'form' %>

<% if params[:product_id]%>
 <%= button_to 'More Info', plan_value_product_info_path(:product_plan_id => ProductPlan.find_all_by_product_id(params[:product_id]).first.id), :method => :get %>
<%end%>
 <%= link_to 'Back', user_product_plans_path %>