<%= form_for(@trackkrhome) do |f| %>
  <% if @trackkrhome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trackkrhome.errors.count, "error") %> prohibited this trackkrhome from being saved:</h2>

      <ul>
      <% @trackkrhome.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
